import React from 'react';

export default function SimpleList(props) {
    return (
        <div>
            <div>{props.a}</div>
            <div>{props.b}</div>
            <div>{props.c}</div>
        </div>
    )
}
